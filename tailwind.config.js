module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {
            colors: {
                primary: 'rgba(255, 152, 0, 0.8)',
            },
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
}